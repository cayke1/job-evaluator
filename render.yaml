services:
  - type: web
    name: job-evaluator-mcp
    runtime: node
    rootDir: mcp
    buildCommand: npm install && npm run build
    startCommand: node dist/index.js
    envVars:
      - key: JOB_EVALUATOR_URL
        value: https://SUBSTITUA-PELA-URL-DO-SEU-RENDER.onrender.com
